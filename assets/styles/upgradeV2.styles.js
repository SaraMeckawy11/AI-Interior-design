import { StyleSheet } from 'react-native';

import COLORS from '../../constants/colors';
import { RADIUS, SHADOW, SPACING, TYPE, ms } from '../../constants/theme';

export default StyleSheet.create({
  screen: { flex: 1, backgroundColor: COLORS.background },
  scrollView: { flex: 1 },
  container: {
    width: '100%',
    maxWidth: 640,
    alignSelf: 'center',
    paddingHorizontal: SPACING.base,
    paddingBottom: SPACING.lg,
  },
  loadingContainer: { flex: 1 },
  centered: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: SPACING.md },
  loadingText: { ...TYPE.small, color: COLORS.textSecondary },

  topBar: {
    minHeight: ms(48),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: SPACING.sm,
  },
  back: {
    width: ms(40),
    height: ms(40),
    borderRadius: RADIUS.pill,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.surface,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  topBarTitle: { ...TYPE.bodyStrong, color: COLORS.textPrimary },
  topBarSpacer: { width: ms(40), height: ms(40) },

  hero: {
    minHeight: ms(194),
    borderRadius: RADIUS.lg,
    padding: SPACING.lg,
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: COLORS.brand200,
    ...SHADOW.xs,
  },
  heroOrb: {
    position: 'absolute',
    width: ms(180),
    height: ms(180),
    borderRadius: RADIUS.pill,
    top: ms(-95),
    right: ms(-70),
    backgroundColor: 'rgba(255,255,255,0.48)',
  },
  heroMetaRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: SPACING.sm,
  },
  heroEyebrow: {
    flex: 1,
    minWidth: 0,
    flexDirection: 'row',
    alignItems: 'center',
    gap: SPACING.xs,
  },
  heroEyebrowText: {
    ...TYPE.overline,
    fontSize: ms(8.5),
    lineHeight: ms(12),
    color: COLORS.primaryDark,
    letterSpacing: 1.1,
  },
  heroTitle: {
    ...TYPE.h1,
    fontSize: ms(24),
    color: COLORS.textPrimary,
    marginTop: SPACING.base,
  },
  heroSubtitle: {
    ...TYPE.small,
    color: COLORS.textSecondary,
    marginTop: 2,
    maxWidth: '92%',
  },
  featureStrip: { flexDirection: 'row', gap: SPACING.sm, marginTop: SPACING.base },
  featureItem: {
    flex: 1,
    minWidth: 0,
    flexDirection: 'row',
    alignItems: 'center',
    gap: SPACING.xs,
    paddingHorizontal: SPACING.sm,
    paddingVertical: SPACING.sm,
    borderRadius: RADIUS.sm,
    backgroundColor: 'rgba(255,255,255,0.68)',
  },
  featureIcon: {
    width: ms(22),
    height: ms(22),
    borderRadius: RADIUS.pill,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.brand100,
  },
  featureText: {
    flex: 1,
    ...TYPE.caption,
    fontSize: ms(8.8),
    lineHeight: ms(12),
    color: COLORS.textSecondary,
  },
  activeTag: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: SPACING.xs,
    borderRadius: RADIUS.pill,
    paddingHorizontal: SPACING.sm,
    paddingVertical: SPACING.xs,
    backgroundColor: COLORS.successSoft,
  },
  activeTagText: { ...TYPE.caption, color: COLORS.success },

  purchaseToggle: {
    flexDirection: 'row',
    gap: SPACING.xs,
    padding: SPACING.xs,
    marginTop: SPACING.lg,
    marginBottom: SPACING.lg,
    borderWidth: 1,
    borderColor: COLORS.border,
    borderRadius: RADIUS.lg,
    backgroundColor: COLORS.surface,
  },
  purchaseToggleItem: {
    flex: 1,
    minHeight: ms(40),
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: RADIUS.md,
  },
  purchaseToggleItemActive: {
    backgroundColor: COLORS.primaryTint,
  },
  purchaseToggleText: { ...TYPE.caption, color: COLORS.textSecondary },
  purchaseToggleTextActive: { color: COLORS.primaryDark },

  sectionHeadingRow: {
    minHeight: ms(28),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: SPACING.md,
    marginBottom: SPACING.sm,
  },
  sectionLabel: { ...TYPE.bodyStrong, color: COLORS.textPrimary },

  optionList: {
    borderWidth: 1,
    borderColor: COLORS.border,
    borderRadius: RADIUS.lg,
    backgroundColor: COLORS.surface,
    overflow: 'hidden',
  },
  optionCard: {
    minHeight: ms(68),
    flexDirection: 'row',
    alignItems: 'center',
    padding: SPACING.md,
    gap: SPACING.md,
    backgroundColor: COLORS.surface,
  },
  optionCardDivider: { borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: COLORS.border },
  optionCardSelected: {
    backgroundColor: COLORS.primaryTint,
  },
  radio: {
    width: ms(21),
    height: ms(21),
    borderRadius: RADIUS.pill,
    borderWidth: 1.5,
    borderColor: COLORS.borderStrong,
    alignItems: 'center',
    justifyContent: 'center',
  },
  radioOn: { borderColor: COLORS.primaryDark, backgroundColor: COLORS.primaryDark },
  optionCopy: { flex: 1, minWidth: 0 },
  optionTitleRow: { flexDirection: 'row', alignItems: 'center', gap: SPACING.sm },
  optionTitle: { flexShrink: 1, ...TYPE.bodyStrong, color: COLORS.textPrimary },
  optionDescription: { ...TYPE.caption, color: COLORS.textSecondary, marginTop: 1 },
  optionPriceWrap: { maxWidth: '34%', minWidth: ms(76), alignItems: 'flex-end' },
  optionPrice: { ...TYPE.bodyStrong, color: COLORS.textPrimary, textAlign: 'right' },
  optionPeriod: { ...TYPE.caption, fontSize: ms(9.5), color: COLORS.textTertiary },
  optionBadge: {
    paddingHorizontal: SPACING.xs,
  },
  optionBadgeText: {
    ...TYPE.overline,
    fontSize: ms(8),
    lineHeight: ms(11),
    color: COLORS.primaryDark,
    letterSpacing: 0.6,
  },

  manageButton: {
    minHeight: ms(44),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.xs,
    marginTop: SPACING.sm,
  },
  manageButtonText: { ...TYPE.caption, color: COLORS.textSecondary },

  balanceCard: {
    minHeight: ms(56),
    flexDirection: 'row',
    alignItems: 'center',
    gap: SPACING.md,
    paddingHorizontal: SPACING.md,
    marginBottom: SPACING.lg,
    borderWidth: 1,
    borderColor: COLORS.border,
    borderRadius: RADIUS.lg,
    backgroundColor: COLORS.surface,
  },
  balanceCopy: { flex: 1, minWidth: 0, flexDirection: 'row', alignItems: 'baseline', gap: SPACING.xs },
  balanceLabel: {
    ...TYPE.caption,
    color: COLORS.textSecondary,
  },
  balanceValue: { ...TYPE.bodyStrong, color: COLORS.textPrimary },
  watchAdButton: {
    minWidth: ms(80),
    minHeight: ms(36),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.xs,
    paddingHorizontal: SPACING.md,
    borderRadius: RADIUS.md,
    backgroundColor: COLORS.accentTint,
  },
  watchAdButtonText: { ...TYPE.caption, color: COLORS.accentStrong },
  adStatusText: {
    ...TYPE.caption,
    color: COLORS.textSecondary,
    textAlign: 'center',
    marginTop: -SPACING.md,
    marginBottom: SPACING.md,
  },
  purchaseFooter: {
    width: '100%',
    backgroundColor: COLORS.background,
    paddingHorizontal: SPACING.base,
    paddingTop: SPACING.sm,
  },
  footerInner: { width: '100%', maxWidth: 640, alignSelf: 'center' },
  primaryButton: {
    width: '100%',
    minHeight: ms(52),
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    ...SHADOW.sm,
  },
  primaryButtonFill: {
    minHeight: ms(52),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: SPACING.md,
    paddingHorizontal: SPACING.lg,
  },
  primaryButtonDisabled: { backgroundColor: COLORS.surfaceSunken, ...SHADOW.none },
  primaryButtonPressed: { opacity: 0.9 },
  primaryButtonText: { flex: 1, ...TYPE.bodyStrong, color: COLORS.white },
  primaryButtonPriceWrap: {
    minHeight: ms(30),
    justifyContent: 'center',
    paddingHorizontal: SPACING.sm,
    borderRadius: RADIUS.md,
    backgroundColor: 'rgba(255,255,255,0.12)',
  },
  primaryButtonPrice: { ...TYPE.bodyStrong, color: COLORS.white, textAlign: 'right' },
  primaryButtonTextDisabled: { ...TYPE.bodyStrong, color: COLORS.textTertiary, textAlign: 'center' },
  footerNote: {
    ...TYPE.caption,
    fontSize: ms(10),
    lineHeight: ms(14.5),
    color: COLORS.textSecondary,
    textAlign: 'center',
    marginTop: SPACING.xs,
  },
  legalLinks: {
    minHeight: ms(32),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.xs,
    marginTop: SPACING.xxs,
  },
  legalLink: {
    minHeight: ms(32),
    justifyContent: 'center',
    paddingHorizontal: SPACING.xs,
  },
  legalLinkText: {
    ...TYPE.caption,
    color: COLORS.primaryDark,
  },
  legalDot: {
    width: ms(3),
    height: ms(3),
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.borderStrong,
  },
  pressed: { opacity: 0.78 },

  dialogBackdrop: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: SPACING.xl,
    backgroundColor: COLORS.scrim,
  },
  dialog: {
    width: '100%',
    maxWidth: 420,
    alignItems: 'center',
    borderRadius: RADIUS.xl,
    padding: SPACING.xl,
    backgroundColor: COLORS.surface,
    ...SHADOW.lg,
  },
  dialogIcon: {
    width: ms(48),
    height: ms(48),
    borderRadius: RADIUS.pill,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.primaryTint,
    marginBottom: SPACING.md,
  },
  dialogTitle: { ...TYPE.h3, color: COLORS.textPrimary, textAlign: 'center' },
  dialogMessage: {
    ...TYPE.small,
    color: COLORS.textSecondary,
    textAlign: 'center',
    marginTop: SPACING.xs,
  },
  dialogButton: {
    width: '100%',
    minHeight: ms(48),
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: RADIUS.md,
    backgroundColor: COLORS.primaryDark,
    marginTop: SPACING.lg,
  },
  dialogButtonText: { ...TYPE.bodyStrong, color: COLORS.white },
});
