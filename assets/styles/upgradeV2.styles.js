import { StyleSheet } from 'react-native';

import COLORS from '../../constants/colors';
import { RADIUS, SHADOW, SPACING, TYPE, ms } from '../../constants/theme';

const card = {
  backgroundColor: COLORS.surface,
  borderWidth: 1,
  borderColor: COLORS.border,
  borderRadius: RADIUS.lg,
};

export default StyleSheet.create({
  screen: { flex: 1, backgroundColor: COLORS.background },
  container: {
    width: '100%',
    maxWidth: 640,
    alignSelf: 'center',
    paddingHorizontal: SPACING.base,
    paddingBottom: SPACING.xl,
  },
  loadingContainer: { flex: 1 },
  centered: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: SPACING.md },
  loadingText: { ...TYPE.small, color: COLORS.textSecondary },

  topBar: {
    minHeight: ms(48),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: SPACING.md,
  },
  back: {
    width: ms(40),
    height: ms(40),
    borderRadius: RADIUS.pill,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.surface,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  topBarTitle: { ...TYPE.bodyStrong, color: COLORS.textPrimary },
  topBarSpacer: { width: ms(40), height: ms(40) },

  hero: {
    minHeight: ms(218),
    borderRadius: RADIUS.xl,
    padding: SPACING.lg,
    overflow: 'hidden',
    ...SHADOW.xs,
  },
  heroGlowOne: {
    position: 'absolute',
    width: ms(170),
    height: ms(170),
    borderRadius: RADIUS.pill,
    backgroundColor: 'rgba(255,255,255,0.045)',
    right: ms(-58),
    top: ms(-82),
  },
  heroGlowTwo: {
    position: 'absolute',
    width: ms(120),
    height: ms(120),
    borderRadius: RADIUS.pill,
    backgroundColor: 'rgba(217,165,131,0.055)',
    left: ms(-48),
    bottom: ms(-58),
  },
  heroEyebrow: {
    flexDirection: 'row',
    alignItems: 'center',
    alignSelf: 'flex-start',
    gap: SPACING.xs,
    backgroundColor: 'rgba(255,255,255,0.08)',
    borderWidth: 1,
    borderColor: 'rgba(255,255,255,0.11)',
    borderRadius: RADIUS.pill,
    paddingHorizontal: SPACING.sm,
    paddingVertical: SPACING.xs,
  },
  heroEyebrowText: {
    ...TYPE.overline,
    fontSize: ms(9.5),
    color: COLORS.textInverse,
    letterSpacing: 1.4,
  },
  heroTitle: { ...TYPE.h1, color: COLORS.textInverse, marginTop: SPACING.md, maxWidth: '90%' },
  heroSubtitle: {
    ...TYPE.small,
    color: 'rgba(246,248,245,0.72)',
    marginTop: SPACING.xs,
    maxWidth: '92%',
  },
  featureStrip: { flexDirection: 'row', gap: SPACING.sm, marginTop: SPACING.base },
  featureItem: { flex: 1, minWidth: 0, gap: SPACING.xs },
  featureIconWrap: {
    width: ms(28),
    height: ms(28),
    borderRadius: RADIUS.sm,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(255,255,255,0.08)',
  },
  featureText: {
    ...TYPE.caption,
    fontSize: ms(9.5),
    lineHeight: ms(14),
    color: 'rgba(246,248,245,0.9)',
  },
  activeTag: {
    position: 'absolute',
    top: SPACING.lg,
    right: SPACING.lg,
    flexDirection: 'row',
    alignItems: 'center',
    gap: SPACING.xs,
    borderRadius: RADIUS.pill,
    paddingHorizontal: SPACING.sm,
    paddingVertical: SPACING.xs,
    backgroundColor: 'rgba(226,240,231,0.13)',
  },
  activeTagText: { ...TYPE.caption, color: COLORS.successSoft },

  purchaseToggle: {
    flexDirection: 'row',
    gap: SPACING.xs,
    padding: SPACING.xs,
    marginTop: SPACING.lg,
    marginBottom: SPACING.lg,
    backgroundColor: COLORS.surfaceSunken,
    borderRadius: RADIUS.md,
  },
  purchaseToggleItem: {
    flex: 1,
    minHeight: ms(44),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.sm,
    borderRadius: RADIUS.sm,
  },
  purchaseToggleItemActive: { backgroundColor: COLORS.surface },
  purchaseToggleText: { ...TYPE.caption, color: COLORS.textSecondary },
  purchaseToggleTextActive: { color: COLORS.primaryDark },
  purchaseToggleTextCoinActive: { color: COLORS.accentStrong },

  sectionHeadingRow: {
    minHeight: ms(42),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: SPACING.md,
    marginBottom: SPACING.md,
  },
  sectionHeadingCopy: { flex: 1, minWidth: 0 },
  sectionLabel: { ...TYPE.h3, color: COLORS.textPrimary },
  sectionHint: { ...TYPE.caption, color: COLORS.textSecondary, marginTop: 1 },
  savingPill: {
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.accentSoft,
    paddingHorizontal: SPACING.sm,
    paddingVertical: SPACING.xs,
  },
  savingPillText: {
    ...TYPE.overline,
    fontSize: ms(9),
    color: COLORS.accentStrong,
    letterSpacing: 0.8,
  },

  optionList: { gap: SPACING.sm },
  optionCard: {
    ...card,
    minHeight: ms(74),
    flexDirection: 'row',
    alignItems: 'center',
    padding: SPACING.md,
    gap: SPACING.md,
    overflow: 'hidden',
  },
  optionCardSelected: {
    borderWidth: 1.5,
    borderColor: COLORS.primaryDark,
    backgroundColor: COLORS.primaryTint,
  },
  coinOptionCardSelected: {
    borderWidth: 1.5,
    borderColor: COLORS.accent,
    backgroundColor: COLORS.accentTint,
  },
  radio: {
    width: ms(20),
    height: ms(20),
    borderRadius: RADIUS.pill,
    borderWidth: 1.5,
    borderColor: COLORS.borderStrong,
    alignItems: 'center',
    justifyContent: 'center',
  },
  radioOn: { borderColor: COLORS.primaryDark },
  radioDot: {
    width: ms(10),
    height: ms(10),
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.primaryDark,
  },
  coinRadioOn: { borderColor: COLORS.accentStrong },
  coinRadioDot: {
    width: ms(10),
    height: ms(10),
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.accentStrong,
  },
  optionCopy: { flex: 1, minWidth: 0 },
  optionTitleRow: { flexDirection: 'row', alignItems: 'center', flexWrap: 'wrap', gap: SPACING.sm },
  optionTitle: { ...TYPE.bodyStrong, color: COLORS.textPrimary },
  optionDescription: { ...TYPE.caption, color: COLORS.textSecondary, marginTop: 1 },
  recommendedBadge: {
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.brand100,
    paddingHorizontal: 7,
    paddingVertical: 2,
  },
  recommendedBadgeText: {
    ...TYPE.overline,
    fontSize: ms(8),
    lineHeight: ms(11),
    color: COLORS.brand700,
    letterSpacing: 0.6,
  },
  optionPriceWrap: { alignItems: 'flex-end' },
  optionPrice: { ...TYPE.bodyStrong, color: COLORS.textPrimary },
  optionPeriod: { ...TYPE.caption, fontSize: ms(10), color: COLORS.textTertiary, marginTop: -1 },

  assuranceRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.sm,
    marginTop: SPACING.base,
  },
  assuranceText: { ...TYPE.caption, color: COLORS.textSecondary },
  manageButton: {
    minHeight: ms(44),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.xs,
    marginTop: SPACING.xs,
  },
  manageButtonText: { ...TYPE.caption, color: COLORS.textSecondary },

  balanceCard: {
    ...card,
    minHeight: ms(72),
    flexDirection: 'row',
    alignItems: 'center',
    padding: SPACING.md,
    gap: SPACING.md,
    marginBottom: SPACING.lg,
  },
  balanceIcon: {
    width: ms(40),
    height: ms(40),
    borderRadius: RADIUS.md,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.accentSoft,
  },
  balanceCopy: { flex: 1, minWidth: 0 },
  balanceLabel: {
    ...TYPE.overline,
    fontSize: ms(9),
    color: COLORS.textTertiary,
    letterSpacing: 1,
  },
  balanceValue: { ...TYPE.h3, color: COLORS.textPrimary },
  watchAdButton: {
    minWidth: ms(82),
    minHeight: ms(40),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.xs,
    paddingHorizontal: SPACING.md,
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.accentSoft,
  },
  watchAdButtonText: { ...TYPE.caption, color: COLORS.accentStrong },
  adStatusText: {
    ...TYPE.caption,
    color: COLORS.textSecondary,
    textAlign: 'center',
    marginTop: -SPACING.md,
    marginBottom: SPACING.md,
  },
  coinAmountIcon: {
    width: ms(30),
    height: ms(30),
    borderRadius: RADIUS.pill,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.accentSoft,
    marginLeft: -SPACING.xs,
  },
  coinBadge: {
    borderRadius: RADIUS.pill,
    backgroundColor: COLORS.accentSoft,
    paddingHorizontal: 7,
    paddingVertical: 2,
  },
  coinBadgeText: {
    ...TYPE.overline,
    fontSize: ms(8),
    lineHeight: ms(11),
    color: COLORS.accentStrong,
    letterSpacing: 0.6,
  },
  coinUseCard: { ...card, padding: SPACING.base, marginTop: SPACING.lg },
  coinUseTitle: { ...TYPE.bodyStrong, color: COLORS.textPrimary, marginBottom: SPACING.md },
  coinUseRow: { minHeight: ms(32), flexDirection: 'row', alignItems: 'center', gap: SPACING.sm },
  coinUseLabel: { flex: 1, ...TYPE.small, color: COLORS.textSecondary },
  coinUseValue: { ...TYPE.caption, color: COLORS.textPrimary },
  coinUseDivider: {
    height: StyleSheet.hairlineWidth,
    backgroundColor: COLORS.divider,
    marginVertical: SPACING.sm,
  },

  purchaseFooter: {
    width: '100%',
    backgroundColor: COLORS.surface,
    borderTopWidth: StyleSheet.hairlineWidth,
    borderTopColor: COLORS.border,
    paddingHorizontal: SPACING.base,
    paddingTop: SPACING.md,
    ...SHADOW.sm,
  },
  summary: {
    width: '100%',
    maxWidth: 640,
    alignSelf: 'center',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: SPACING.md,
    marginBottom: SPACING.sm,
  },
  summaryCopy: { flex: 1, minWidth: 0 },
  summaryEyebrow: {
    ...TYPE.overline,
    fontSize: ms(8.5),
    lineHeight: ms(11),
    color: COLORS.textTertiary,
    letterSpacing: 1,
  },
  summaryLabel: { ...TYPE.small, color: COLORS.textPrimary },
  summaryValue: { ...TYPE.h3, color: COLORS.textPrimary },
  primaryButton: {
    width: '100%',
    maxWidth: 640,
    alignSelf: 'center',
    minHeight: ms(50),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: SPACING.sm,
    borderRadius: RADIUS.md,
    backgroundColor: COLORS.primaryDark,
  },
  primaryButtonDisabled: { backgroundColor: COLORS.surfaceSunken },
  primaryButtonText: { ...TYPE.bodyStrong, color: COLORS.white },
  pressed: { opacity: 0.78 },

  dialogBackdrop: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: SPACING.xl,
    backgroundColor: COLORS.scrim,
  },
  dialog: {
    width: '100%',
    maxWidth: 420,
    alignItems: 'center',
    borderRadius: RADIUS.xl,
    padding: SPACING.xl,
    backgroundColor: COLORS.surface,
    ...SHADOW.lg,
  },
  dialogIcon: {
    width: ms(48),
    height: ms(48),
    borderRadius: RADIUS.pill,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.primaryTint,
    marginBottom: SPACING.md,
  },
  dialogTitle: { ...TYPE.h3, color: COLORS.textPrimary, textAlign: 'center' },
  dialogMessage: {
    ...TYPE.small,
    color: COLORS.textSecondary,
    textAlign: 'center',
    marginTop: SPACING.xs,
  },
  dialogButton: {
    width: '100%',
    minHeight: ms(48),
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: RADIUS.md,
    backgroundColor: COLORS.primaryDark,
    marginTop: SPACING.lg,
  },
  dialogButtonText: { ...TYPE.bodyStrong, color: COLORS.white },
});
